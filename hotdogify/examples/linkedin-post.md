# LinkedIn post — to caption the before → after

> Mode B output. Pair it with a single image: the original keypad on the left, the
> hotdogified one on the right. The picture carries it; the copy just sets it up.

---

The next time someone tells you "taste is the moat," show them this.

[ before → after: a calculator, optimized ]

So I built a skill that makes a UI worse on purpose and then writes the deck that calls it a win.

On this calculator it:
→ rotated the hamburger menu 90° into a hot dog
→ shuffled the keypad so position no longer predicts the number
→ moved `=` to the top, away from your thumb
→ swapped the `+` and `−` seats
→ bolted on a time-in-app counter that only goes up

Time-in-app is up 34%. Nobody can find the 7. Working as intended.

The math still works perfectly. That was never the problem. The problem is that we keep
calling motion "engagement" and engagement "taste," and nobody checks.

Built with 🌭 hotdogify. We do the opposite, for money.

#yempik #ux #ai #buildinpublic
